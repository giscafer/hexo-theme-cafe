<% if (!is_tag() && site.posts.length){ %>
  <div class="widget-wrap">
    <h3 class="widget-title archive"><%= __('archive_a') %></h3>
    <div class="widget">
      <%- list_archives({show_count: theme.show_count, type: theme.archive_type}) %>
    </div>
  </div>
<% } %>
